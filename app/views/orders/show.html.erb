<div class="container">
  <div class="row">
    <div class="col-sm-3">
      <div class="customer-info">
        <%= @order.name %>
        <%= @order.people %>
        <%= @order.total_price %>
        <% @dishes.each do |d| %>
          <%= d.name %>
        <% end %> 
        <% @order_detais.each do |b| %>
          <%= b.quantity %>
        <% end %>
      </div>
    </div>
    <div class="col-sm-9">
      <div class="customer-activities">
      </div>
    </div>
  </div>
</div>
